services:
  goal-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goal-manager-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
